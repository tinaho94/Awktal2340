apply plugin: 'java'
apply plugin: 'application'
defaultTasks 'run'

mainClassName = 'awkMULE.MainApp'
version = '1.0'

jar {
  manifest {
    attributes('Implementation-Title': 'Awk MULE',
               'Implementation-Version': version,
               'Main-CLass': mainClassName,
    )
  }
}

repositories {
  mavenCentral()
}

dependencies {
}